# Getting started

1. Clone this repo
2. Run `docker compose up -d`
3. Load the market data in `/market-data/XNAS/2026FEB18.csv` by running the `add-datasource-XNAS-2026FEB18.sh` script in `/api-requests`
4. Restart the instance to reload the data.
